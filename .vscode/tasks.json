{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "key": "ctrl+h",
            "command": "${command:arduino.upload} && bash",
            "args": [""]
        }
    ]
}